package com.monetization.core.msgs

enum class MessagesType {
    NoController,
    AdNotEnabled,
    AdNotAvailable,
    ShowingNotAllowed,
    AdLoadFailed,
    AdLoadedButNoListeners,
    InstantTimeEnd,
    CounterNotReached,
}